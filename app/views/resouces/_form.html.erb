<%= form_for(resouce) do |f| %>
  <% if resouce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resouce.errors.count, "error") %> prohibited this resouce from being saved:</h2>

      <ul>
      <% resouce.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
